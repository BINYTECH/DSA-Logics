Array : An Array is a collection of elements with same datatype stored in a contiguos memory location.


//swap function for swapping elements in an array
public static void swap(int arr[],index1, index2)
{
int temp=arr[index1];
arr[index1]=arr[index2];
arr[index2]=temp;
}

//Sum of all the array elements
int arr[] ={3,8,5,12,11};
int sum=0;
for(int i=0;i<arr.length;i++)
{
sum +=arr[i];
}
System.out.prinltn(sum); ========> Output = 39

//Sum of Array elements using for-each loop
int arr[] ={3,8,5,12,11};
int sum=0;
for(int j : arr)
{
sum += j;
}
System.out.prinltn(sum); ========> Output = 39

//Find largest element in an Array
int arr[] ={45,23,89,32,67};
int max=arr[0]; Set starting array element to be the max and then do the comparison with all other elements
//int max=Integer.MIN_VALUE;  Integer.MIN_VALUE stores minimum possible value for any integer variable

for(int i=0;i<arr.length;i++){
  if(arr[i]>max){
      max=arr[i];
      }
    }
System.out.println(max);
