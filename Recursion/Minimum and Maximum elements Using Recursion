Minimum and Maximum elements Using Recursion

Given an array of integers arr[], find the minimum and maximum elements in the array using recursion only.
The first element of the output represents the minimum value, and the second element represents the maximum value in the array.

Examples:
Input: arr[] = [1, 4, 3, -5, -4, 8, 6]
Output: [-5, 8]
Explanation: -5 is the minimum and 8 is the maximum element in the array

Input: arr[] = [12, 3, 15, 7, 9]
Output: [3, 15]
Explanation: 3 is the minimum and 15 is the maximum element in the array


Brute Force Approach: 
Consider first element to be min element and max element as well.
Then run a loop to check all the elements with min element and max element and update accordingly;
Create an array of two size and add min element at place 0 index and max element at 1 index and return the array.

Solution:
public class Main{
    public static void main(String args[]){
        int arr[]={ -1,-2,-3,-4};int m=3;
        //System.out.println(Subarray_Diff_Integer(arr,m));
        System.out.println(Arrays.toString(Max_Min_Value(arr)));
    }
   public static int[] Max_Min_Value(int arr[]){
     int max=arr[0];
     int min=arr[0];
     int Max_Min_Value[]=new int[2];

     for(int i=0;i<arr.length;i++){
      if(arr[i]<min){
        min=arr[i];
      }
      if(arr[i]>max){
        max=arr[i];
      }
     }
     Max_Min_Value[0]=min;
     Max_Min_Value[1]=max;
     return Max_Min_Value;
   }
}


Recursively Using Helper function:

Solution:
public class Main{
    public static void main(String args[]){
        int arr[]={ 1,2,3,4};int m=3;
        //System.out.println(Subarray_Diff_Integer(arr,m));
        //ArrayList<Integer> res=Max_Min_Value(arr);
        System.out.println(Max_Min_Value(arr));
    }
    public static ArrayList<Integer> Max_Min_Value(int arr[]){
      return find_Max_Min(arr,arr.length-1);
    }
   public static ArrayList<Integer> find_Max_Min(int arr[],int n){

    //Base Condition
    if(n==1){
      ArrayList<Integer> base=new ArrayList<>();
      base.add(arr[0]);
      base.add(arr[0]);
      return base;
    }

    ArrayList<Integer> result=find_Max_Min(arr,n-1);

    //Update min
     if(arr[n]< result.get(0)){
      result.set(0,arr[n]);
     }
     //Update Max
     if(arr[n]>result.get(1)){
      result.set(1,arr[n]);
     }    
     return result;
   }
}




